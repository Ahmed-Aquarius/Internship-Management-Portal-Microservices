# import .env
spring.config.import=optional:env[.env]

# web data
spring.application.name=task-service
server.port=8086

# Database connection
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/task_db?useUnicode=true&characterEncoding=utf8}
spring.datasource.username=${DB_USERNAME:ejada}
spring.datasource.password=${DB_PASSWORD:ejada123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# JWT
jwt.secret=66178a71d8b3dbff6fd840f888bf8b9f4f32f10787e35eafaf9d2414a1b2c3d4e5f6
jwt.expiration=86400000